$ADDITIONAL_USING$

using System;

using GameCore;

using UnityEngine;

namespace $NAMESPACE_NAME$ 
{
	[CreateAssetMenu (fileName = "$NAME_CONDITION$Condition", menuName = "$NAMESPACE_NAME$ /EventCondition/$NAME_CONDITION$")]
    public class $NAME_CONDITION$EventCondition : EventCondition<$NAME_CONDITION$> { }
}
