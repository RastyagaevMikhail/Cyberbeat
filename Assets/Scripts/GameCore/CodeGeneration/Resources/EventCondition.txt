$ADDITIONAL_USING$

using System;

using GameCore;

using UnityEngine;

namespace $NAMESPACE_NAME$ 
{
	[CreateAssetMenu (fileName = "$TYPE_CONDITION$Condition", menuName = "$NAMESPACE_NAME$/EventCondition/$TYPE_CONDITION$")]
	public class $TYPE_CONDITION$EventCondition : EventCondition<$TYPE_CONDITION$> { }
}
